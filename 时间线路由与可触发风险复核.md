# 时间线路由与可触发风险复核报告

## 复核概述

本报告基于代码实事，对 Plane Issue 时间线链路的三个问题进行复核：

1. API 路由矩阵：issues、work-items、epics 各自的 history 端点路径映射
2. 前端 `serviceType=EPICS` 时的实际请求路径与返回结果语义
3. 默认分支风险的可触发场景与优先级结论

---

## 一、API 路由矩阵

### 1.1 路由前缀映射

**文件**：`apps/api/plane/urls.py:18-21`

```python
urlpatterns = [
    path("api/", include("plane.app.urls")),         # ✅ 前端使用的端点
    path("api/public/", include("plane.space.urls")), # 公开部署面板
    path("api/v1/", include("plane.api.urls")),       # API 文档中的 v1 端点
]
```

### 1.2 完整路由矩阵

| 实体类型 | URL 路径 | 后端 View | 前缀 | 说明 |
|---------|---------|-----------|------|------|
| **issues** | `/api/workspaces/{slug}/projects/{project_id}/issues/{issue_id}/history/` | `IssueActivityEndpoint` | `api/` | 支持 `activity_type` 参数，可合并活动+评论 |
| **issues** | `/api/workspaces/{slug}/projects/{project_id}/issues/{issue_id}/comments/` | `IssueCommentViewSet` | `api/` | 独立评论 CRUD |
| **issues** | `/api/v1/workspaces/{slug}/projects/{project_id}/issues/{issue_id}/activities/` | `IssueActivityListAPIEndpoint` | `api/v1/` | 仅活动列表，分页 API |
| **issues** | `/api/v1/workspaces/{slug}/projects/{project_id}/issues/{issue_id}/comments/` | `IssueCommentListCreateAPIEndpoint` | `api/v1/` | 仅评论列表，分页 API |
| **work-items** | `/api/v1/workspaces/{slug}/projects/{project_id}/work-items/{issue_id}/activities/` | `IssueActivityListAPIEndpoint` | `api/v1/` | 与 issues 复用同一 View |
| **work-items** | `/api/v1/workspaces/{slug}/projects/{project_id}/work-items/{issue_id}/comments/` | `IssueCommentListCreateAPIEndpoint` | `api/v1/` | 与 issues 复用同一 View |
| **epics** | ❌ **无独立路由** | ❌ | - | 后端未定义 `/epics/.../history/` 路由 |

### 1.3 两个活动端点的本质区别

| 维度 | `/history/` 端点（app 层） | `/activities/` 端点（api/v1 层） |
|-----|---------------------------|--------------------------------|
| **文件** | `app/views/issue/activity.py` | `api/views/issue.py:1654` |
| **View 类** | `IssueActivityEndpoint` | `IssueActivityListAPIEndpoint` |
| **支持活动类型** | 活动 + 评论（可合并） | 仅活动（排除评论等） |
| **分页** | ❌ 无分页 | ✅ 游标分页（`cursor` / `per_page`） |
| **过滤参数** | `activity_type`, `created_at__gt` | `order_by`, `fields`, `expand` |
| **合并排序** | ✅ 活动与评论按 `created_at` 合并排序 | ❌ 仅活动 |
| **前端使用** | ✅ Web App 时间线使用 | ❌ Web App 不使用 |
| **API 文档** | ❌ 未在 OpenAPI 文档中 | ✅ 完整 OpenAPI 文档 |

### 1.4 `/history/` 端点的 `activity_type` 参数分支

**文件**：`app/views/issue/activity.py:66-84`

```python
# 分支 A：只返回属性活动
if request.GET.get("activity_type", None) == "issue-property":
    issue_activities = IssueActivitySerializer(issue_activities, many=True).data
    return Response(issue_activities)

# 分支 B：只返回评论
if request.GET.get("activity_type", None) == "issue-comment":
    issue_comments = IssueCommentSerializer(issue_comments, many=True).data
    return Response(issue_comments)

# 默认分支：合并返回（有 Bug）
result_list = sorted(
    chain(issue_activities, issue_comments),
    key=lambda instance: instance["created_at"],  # ❌ TypeError
)
return Response(result_list)
```

---

## 二、前端 `serviceType=EPICS` 时的实际请求路径与返回语义

### 2.1 `EIssueServiceType` 枚举定义

**文件**：`packages/types/src/issues/issue.ts:23-27`

```typescript
export enum EIssueServiceType {
  ISSUES = "issues",
  EPICS = "epics",
  WORK_ITEMS = "work-items",
}
```

### 2.2 `IssueActivityService` 的 URL 构造

**文件**：`apps/web/core/services/issue/issue_activity.service.ts:17-42`

```typescript
export class IssueActivityService extends APIService {
  private serviceType: TIssueServiceType;

  constructor(serviceType: TIssueServiceType = EIssueServiceType.ISSUES) {
    super(API_BASE_URL);
    this.serviceType = serviceType;
  }

  async getIssueActivities(workspaceSlug, projectId, issueId, params = {}) {
    return this.get(
      `/api/workspaces/${workspaceSlug}/projects/${projectId}/${this.serviceType}/${issueId}/history/`,
      //                                                         ^^^^^^^^^^^^^^^^
      //                                                         serviceType 直接拼入 URL
      {
        params: {
          activity_type: `${this.serviceType === EIssueServiceType.EPICS ? "epic-property" : "issue-property"}`,
          ...params,
        },
      }
    )
  }
}
```

### 2.3 Store 实例化

**文件**：`apps/web/core/store/issue/root.store.ts:238-239`

```typescript
this.issueDetail = new IssueDetail(this, EIssueServiceType.ISSUES);
this.epicDetail = new IssueDetail(this, EIssueServiceType.EPICS);
```

### 2.4 实际请求路径与返回结果对照表

| serviceType | 实际请求 URL | `activity_type` 参数值 | 后端路由匹配 | 返回结果 |
|------------|------------|-----------------------|------------|---------|
| `ISSUES` | `/api/.../issues/{issueId}/history/` | `"issue-property"` | ✅ 匹配路由 | ✅ 分支 A，正常返回活动列表 |
| `ISSUES`（评论） | `/api/.../issues/{issueId}/history/` | `"issue-comment"` | ✅ 匹配路由 | ✅ 分支 B，正常返回评论列表 |
| `EPICS` | `/api/.../epics/{issueId}/history/` | `"epic-property"` | ❌ **路由不匹配** | 🔴 **404 Not Found** |
| `WORK_ITEMS` | `/api/.../work-items/{issueId}/history/` | `"issue-property"` | ❌ 路由不匹配 | 🔴 **404 Not Found** |

### 2.5 关键发现：Epic 时间线的实际状态

**代码实事**：
1. 前端 `epicDetail` Store 用 `serviceType=EPICS` 实例化
2. `IssueActivityService` 将 `serviceType` 直接拼入 URL 路径 → `/epics/{issueId}/history/`
3. 后端 `app/urls/issue.py` 中**没有定义 `/epics/` 前缀的路由**
4. 后端只有 `/issues/.../history/` 路由

**结论**：`serviceType=EPICS` 时，请求在 **Django URL 路由匹配阶段就失败了**，返回 `404 Not Found`，**根本不会走到 `IssueActivityEndpoint` 的默认分支**。

---

## 三、默认分支风险的可触发场景与优先级

### 3.1 触发条件链

要触发默认分支的 `TypeError`，必须**同时满足**以下所有条件：

| 条件 | 说明 | 是否满足 |
|-----|------|---------|
| ✅ 路由匹配 | URL 必须是 `/api/.../issues/{issueId}/history/` | - |
| ✅ `activity_type` 参数 | 参数值**不能是** `"issue-property"` 也不能是 `"issue-comment"` | - |
| ✅ 数据存在 | 该 Issue 下必须存在 `IssueActivity` 或 `IssueComment` 记录 | - |

### 3.2 前端调用链分析

#### 调用方 A：`IssueActivityService.getIssueActivities`

**文件**：`issue_activity.service.ts:34`

```typescript
activity_type: `${this.serviceType === EIssueServiceType.EPICS ? "epic-property" : "issue-property"}`,
```

- 当 `serviceType=ISSUES`：`activity_type="issue-property"` → ✅ 分支 A
- 当 `serviceType=EPICS`：`activity_type="epic-property"` → ❌ 404（路由不匹配）
- **不会走到默认分支**

#### 调用方 B：`IssueCommentService.getIssueComments`

**文件**：`issue_comment.service.ts:38`

```typescript
activity_type: `${this.serviceType === EIssueServiceType.EPICS ? "epic-comment" : "issue-comment"}`,
```

- 当 `serviceType=ISSUES`：`activity_type="issue-comment"` → ✅ 分支 B
- 当 `serviceType=EPICS`：`activity_type="epic-comment"` → ❌ 404（路由不匹配）
- **不会走到默认分支**

#### 调用方 C：`IssueService.getIssueActivities`

**文件**：`issue.service.ts:139-145`

```typescript
async getIssueActivities(workspaceSlug, projectId, issueId) {
    return this.get(
        `/api/workspaces/${workspaceSlug}/projects/${projectId}/${this.serviceType}/${issueId}/history/`
        // 不传 activity_type 参数！
    )
}
```

- 当 `serviceType=ISSUES`：URL 匹配 ✅，无 `activity_type` → ✅ **触发默认分支**
- 当 `serviceType=EPICS`：URL 不匹配 ❌ → 404
- **代码存在但无调用方**（经全局搜索确认）

### 3.3 可触发场景清单

| 场景 | 触发路径 | 风险级别 | 说明 |
|-----|---------|---------|------|
| **场景 1**：第三方 API 客户端不传 `activity_type` | 手动请求 `/history/` 不带参数 | 🔴 **高** | 必触发 `TypeError` 500 |
| **场景 2**：第三方 API 客户端传错 `activity_type` | 传 `activity_type=invalid` | 🔴 **高** | 必触发 `TypeError` 500 |
| **场景 3**：`IssueService.getIssueActivities` 被调用 | 若某处调用该方法 | 🔴 **高** | 必触发 `TypeError` 500 |
| **场景 4**：`serviceType=EPICS` 时 | `/epics/.../history/` | 🟡 **中** | 返回 404，不触发默认分支 |
| **场景 5**：前端正常使用 | `IssueActivityService` / `IssueCommentService` | 🟢 **低** | 传正确的 `activity_type`，不会触发 |

### 3.4 优先级结论

基于代码实事的优先级评估：

| 优先级 | 依据 |
|-------|------|
| **P2 - 中** | 前端正常使用不会触发，仅第三方客户端或未来代码变更可能触发 |

**理由**：
1. ✅ 前端两个核心 Service（`IssueActivityService`、`IssueCommentService`）都硬编码了正确的 `activity_type` 值，正常使用不会触发
2. ✅ `serviceType=EPICS` 场景返回 404，不会走到默认分支的 TypeError
3. ❗ 但默认分支确实是死代码 Bug，第三方 API 客户端调用会直接 500
4. ❗ `IssueService.getIssueActivities` 方法存在但未使用，是潜在风险点

### 3.5 与之前结论的差异修正

| 之前结论（推测） | 当前结论（代码实事） |
|-----------------|-------------------|
| Epic 场景触发默认分支 TypeError | ❌ Epic 场景路由不匹配，返回 404，不会走到 View 层 |
| Epic 时间线功能不可用（TypeError） | ✅ Epic 时间线功能不可用，但原因是 404，不是 TypeError |
| 默认分支高优先级 | ⚠️ 默认分支是 P2 优先级，前端不触发 |

---

## 四、核心发现汇总

### 4.1 路由层发现

| 发现 | 证据文件 |
|-----|---------|
| `/history/` 端点属于 `api/` 前缀（不是 `api/v1/`） | `urls.py:18` |
| `/activities/` 端点属于 `api/v1/` 前缀，Web App 不使用 | `work_item.py:88-154` |
| **无 `/epics/.../history/` 路由** | 所有 `urls/*.py` 搜索无匹配 |
| Epic 时间线请求返回 404，不是 TypeError | 路由匹配失败在 View 调用之前 |

### 4.2 触发面修正

| 触发场景 | 之前 | 当前（代码实事） |
|---------|------|---------------|
| 前端正常使用 | ❌ 不会触发 | ✅ 不会触发 |
| Epic 时间线 | ❌ TypeError 500 | ✅ 404 Not Found |
| 不传 `activity_type` 参数 | ✅ TypeError 500 | ✅ TypeError 500 |
| 传错 `activity_type` 值 | ✅ TypeError 500 | ✅ TypeError 500 |

### 4.3 优先级建议

| 建议 | 理由 |
|-----|------|
| **修复默认分支** | 虽然前端不触发，但死代码 Bug 应修复，避免第三方调用失败 |
| **评估 Epic 时间线需求** | 当前 Epic 时间线返回 404，需确认是"未实现"还是"应该复用 issues 路由" |
| **移除死代码** | 若 Epic 不需要独立时间线，可考虑移除 Service 中的 EPICS 分支避免误导 |

---

## 五、核心文件索引

| 证据 | 文件路径 | 行号 |
|------|---------|------|
| 路由前缀映射 | `apps/api/plane/urls.py` | 18-21 |
| `/history/` 路由定义 | `apps/api/plane/app/urls/issue.py` | 149-153 |
| `IssueActivityEndpoint` 实现 | `apps/api/plane/app/views/issue/activity.py` | 24-86 |
| 默认分支 Bug | `apps/api/plane/app/views/issue/activity.py` | 81-84 |
| `IssueActivityListAPIEndpoint` 实现 | `apps/api/plane/api/views/issue.py` | 1654-1703 |
| `/work-items/` 路由 | `apps/api/plane/api/urls/work_item.py` | 88-154 |
| `EIssueServiceType` 枚举 | `packages/types/src/issues/issue.ts` | 23-27 |
| `IssueActivityService` URL 构造 | `apps/web/core/services/issue/issue_activity.service.ts` | 32-35 |
| `IssueCommentService` URL 构造 | `apps/web/core/services/issue/issue_comment.service.ts` | 36-39 |
| `IssueService.getIssueActivities`（死代码） | `apps/web/core/services/issue/issue.service.ts` | 139-145 |
| Store 实例化 | `apps/web/core/store/issue/root.store.ts` | 238-239 |
