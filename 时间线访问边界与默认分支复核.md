# 时间线访问边界与默认分支复核报告

## 复核概述

本报告对 Plane Issue 时间线链路的三个边界问题进行代码级复核：

1. `comment.access` 字段的前端使用边界——哪些组件已使用、哪些环节没有
2. History 接口默认分支的触发面——不带或传错 `activity_type` 时的实际执行路径
3. 默认分支修复思路的代码级对比——"属性排序后 Response" vs "先序列化再返回"

---

## 一、`comment.access` 字段的前端使用边界

### 1.1 类型定义

**文件**：`packages/types/src/issues/activity/issue_comment.ts:47`

```typescript
export type TIssueComment = {
  ...
  access: EIssueCommentAccessSpecifier;  // ✅ 必填字段（非 optional）
};
```

**文件**：`packages/types/src/enums.ts:36-39`

```typescript
export enum EIssueCommentAccessSpecifier {
  EXTERNAL = "EXTERNAL",
  INTERNAL = "INTERNAL",
}
```

**文件**：`packages/constants/src/issue/common.ts:55-58`

```typescript
export enum EIssueCommentAccessSpecifier {
  EXTERNAL = "EXTERNAL",
  INTERNAL = "INTERNAL",
}
```

> `access` 在类型定义中为**必填字段**，后端默认值为 `"INTERNAL"`，序列化器使用 `fields = "__all__"` 必然返回。

### 1.2 已使用 `comment.access` 的组件

#### 组件 A：`CommentCardDisplay`（评论显示卡片）

**文件**：`apps/web/core/components/comments/card/display.tsx:110-118`

```tsx
{showAccessSpecifier && (
  <div className="absolute top-2.5 right-2.5 z-[1] text-tertiary">
    {comment.access === EIssueCommentAccessSpecifier.INTERNAL ? (
      <LockIcon className="size-3" />      // 内部评论 → 🔒 锁图标
    ) : (
      <GlobeIcon className="size-3" />      // 外部评论 → 🌐 地球图标
    )}
  </div>
)}
```

**用途**：根据 `access` 值显示不同的图标标识（锁/地球），提示用户该评论的可见范围。

#### 组件 B：`CommentQuickActions`（评论快捷操作菜单）

**文件**：`apps/web/core/components/comments/quick-actions.tsx:58-73`

```tsx
{
  key: "access_specifier",
  action: () =>
    activityOperations.updateComment(comment.id, {
      access:
        comment.access === EIssueCommentAccessSpecifier.INTERNAL
          ? EIssueCommentAccessSpecifier.EXTERNAL     // INTERNAL → 切换为 EXTERNAL
          : EIssueCommentAccessSpecifier.INTERNAL,     // EXTERNAL → 切换为 INTERNAL
    }),
  title:
    comment.access === EIssueCommentAccessSpecifier.INTERNAL
      ? t("issue.comments.switch.public")             // "切换为公开"
      : t("issue.comments.switch.private"),           // "切换为私有"
  icon: comment.access === EIssueCommentAccessSpecifier.INTERNAL ? GlobeIcon : LockIcon,
  shouldRender: showAccessSpecifier,                  // ✅ 受 showAccessSpecifier 控制
},
```

**用途**：提供切换评论可见性的交互操作，将 INTERNAL ↔ EXTERNAL 互相切换。

#### 组件 C：`CommentCreate`（评论创建表单）

**文件**：`apps/web/core/components/comments/comment-create.tsx:109-134`

```tsx
<Controller
  name="access"
  control={control}
  render={({ field: { onChange: onAccessChange, value: accessValue } }) => (
    <Controller
      name="comment_html"
      control={control}
      render={({ field: { value, onChange } }) => (
        <LiteTextEditor
          ...
          accessSpecifier={accessValue ?? EIssueCommentAccessSpecifier.INTERNAL}
          handleAccessChange={onAccessChange}
          ...
        />
      )}
    />
  )}
/>
```

**用途**：创建评论时，编辑器工具栏提供 access 选择器，默认值为 `INTERNAL`。

#### 组件 D：`LiteTextEditorToolbar`（编辑器工具栏）

**文件**：`apps/web/core/components/editor/lite-text/toolbar.tsx:107`

```tsx
{showAccessSpecifier && (
  // 渲染 access 切换按钮
)}
```

**用途**：在评论编辑器工具栏中渲染 access 切换按钮。

### 1.3 未使用 `comment.access` 的环节

| 环节 | 文件路径 | 说明 |
|-----|---------|------|
| **后端时间线查询** | `app/views/issue/activity.py:47-64` | 查询 `IssueComment` 时不过滤 `access`，INTERNAL + EXTERNAL 全部返回 |
| **后端评论 CRUD 查询** | `app/views/issue/comment.py:35-61` | `get_queryset()` 不过滤 `access` |
| **前端 Activity Store** | `ce/store/issue/issue-details/activity.store.ts` | 合并映射时不区分 `access` |
| **前端 Comment Store** | `core/store/issue/issue-details/comment.store.ts` | 拉取评论时不传 `access` 过滤参数 |
| **前端筛选过滤器** | `packages/constants/src/issue/filter.ts` | `EActivityFilterType` 枚举不包含 `access` 类型 |
| **前端时间线渲染** | `activity-comment-root.tsx:65` | 判断 `activity_type === "COMMENT"` 后直接渲染，不检查 `access` |

### 1.4 `showAccessSpecifier` 控制逻辑

**文件**：`apps/web/core/components/issues/issue-detail/issue-activity/root.tsx:92,143`

```tsx
const project = getProjectById(projectId);
...
<IssueActivityCommentRoot
  ...
  showAccessSpecifier={!!project.anchor}  // ✅ 仅当项目有部署面板（anchor）时启用
/>
```

**传递链**：
```
root.tsx: showAccessSpecifier={!!project.anchor}
  → activity-comment-root.tsx: showAccessSpecifier={!!showAccessSpecifier}
    → CommentCard: showAccessSpecifier={showAccessSpecifier}
      → CommentCardDisplay: {showAccessSpecifier && (<LockIcon/GlobeIcon/>)}
      → CommentQuickActions: shouldRender: showAccessSpecifier
```

**事实**：
- `project.anchor` 是项目关联的 DeployBoard 的锚点标识
- **没有部署面板的项目**：`showAccessSpecifier = false`，access 相关 UI 全部隐藏
- **有部署面板的项目**：`showAccessSpecifier = true`，显示图标和切换菜单

### 1.5 使用边界总结

| 层次 | 是否使用 `access` | 使用方式 | 条件 |
|-----|-----------------|---------|------|
| **类型定义** | ✅ | `access: EIssueCommentAccessSpecifier`（必填） | 无条件 |
| **后端模型** | ✅ | 默认值 `"INTERNAL"` | 无条件 |
| **后端序列化** | ✅ | `fields = "__all__"` 包含 `access` | 无条件 |
| **后端查询过滤** | ❌ | 时间线和评论 API 不过滤 `access` | 无条件 |
| **前端图标标识** | ✅ | LockIcon / GlobeIcon | `showAccessSpecifier = true` |
| **前端切换操作** | ✅ | INTERNAL ↔ EXTERNAL 切换 | `showAccessSpecifier = true` |
| **前端创建表单** | ✅ | 编辑器工具栏选择器 | `showAccessSpecifier = true` |
| **前端筛选过滤** | ❌ | 不支持按 `access` 过滤评论 | 无条件 |
| **前端列表过滤** | ❌ | 时间线不区分 INTERNAL/EXTERNAL | 无条件 |

---

## 二、History 接口默认分支的触发面

### 2.1 后端代码执行路径分析

**文件**：`apps/api/plane/app/views/issue/activity.py:30-86`

```python
def get(self, request, slug, project_id, issue_id):
    # ① 构建两个 QuerySet（总是执行）
    issue_activities = (IssueActivity.objects.filter(...)).order_by("created_at")   # QuerySet
    issue_comments = (IssueComment.objects.filter(...)).order_by("created_at")     # QuerySet

    # ② 分支 A：activity_type == "issue-property"
    if request.GET.get("activity_type", None) == "issue-property":
        issue_activities = IssueActivitySerializer(issue_activities, many=True).data  # 序列化
        return Response(issue_activities, status=status.HTTP_200_OK)

    # ③ 分支 B：activity_type == "issue-comment"
    if request.GET.get("activity_type", None) == "issue-comment":
        issue_comments = IssueCommentSerializer(issue_comments, many=True).data      # 序列化
        return Response(issue_comments, status=status.HTTP_200_OK)

    # ④ 默认分支：无 activity_type 或值不匹配
    result_list = sorted(
        chain(issue_activities, issue_comments),        # 合并两个 QuerySet
        key=lambda instance: instance["created_at"],    # ❌ Model 实例字典访问
    )
    return Response(result_list, status=status.HTTP_200_OK)
```

### 2.2 不同 `activity_type` 值的实际执行路径

| 请求参数 | 执行分支 | 数据类型 | `instance["created_at"]` | 结果 |
|---------|---------|---------|------------------------|------|
| `activity_type=issue-property` | 分支 A | `ReturnList[dict]` | 不执行 | ✅ 正常返回 |
| `activity_type=issue-comment` | 分支 B | `ReturnList[dict]` | 不执行 | ✅ 正常返回 |
| `activity_type=epic-property` | 默认分支 ④ | `QuerySet + QuerySet` | ❌ Model 不支持 `__getitem__` | 🔴 `TypeError` 500 |
| `activity_type=epic-comment` | 默认分支 ④ | `QuerySet + QuerySet` | ❌ 同上 | 🔴 `TypeError` 500 |
| `activity_type=anything` | 默认分支 ④ | `QuerySet + QuerySet` | ❌ 同上 | 🔴 `TypeError` 500 |
| 不传 `activity_type` | 默认分支 ④ | `QuerySet + QuerySet` | ❌ 同上 | 🔴 `TypeError` 500 |

### 2.3 前端调用路径逐项核实

#### 路径 A：`IssueActivityService.getIssueActivities`（Activity Store 使用）

**文件**：`apps/web/core/services/issue/issue_activity.service.ts:22-42`

```typescript
async getIssueActivities(workspaceSlug, projectId, issueId, params = {}) {
    return this.get(
        `/api/workspaces/${workspaceSlug}/projects/${projectId}/${this.serviceType}/${issueId}/history/`,
        {
            params: {
                activity_type: `${this.serviceType === EIssueServiceType.EPICS
                    ? "epic-property"      // ⚠️ 后端不识别此值！
                    : "issue-property"}`,
                ...params,
            },
        }
    )
}
```

**分析**：
- 当 `serviceType = EIssueServiceType.ISSUES` 时：传 `activity_type=issue-property` → ✅ 走分支 A
- 当 `serviceType = EIssueServiceType.EPICS` 时：传 `activity_type=epic-property` → ❌ 后端不识别，走默认分支 ④

#### 路径 B：`IssueCommentService.getIssueComments`（Comment Store 使用）

**文件**：`apps/web/core/services/issue/issue_comment.service.ts:26-46`

```typescript
async getIssueComments(workspaceSlug, projectId, issueId, params = {}) {
    return this.get(
        `/api/workspaces/${workspaceSlug}/projects/${projectId}/${this.serviceType}/${issueId}/history/`,
        {
            params: {
                activity_type: `${this.serviceType === EIssueServiceType.EPICS
                    ? "epic-comment"       // ⚠️ 后端不识别此值！
                    : "issue-comment"}`,
                ...params,
            },
        }
    )
}
```

**分析**：
- 当 `serviceType = EIssueServiceType.ISSUES` 时：传 `activity_type=issue-comment` → ✅ 走分支 B
- 当 `serviceType = EIssueServiceType.EPICS` 时：传 `activity_type=epic-comment` → ❌ 后端不识别，走默认分支 ④

#### 路径 C：`IssueService.getIssueActivities`（未使用）

**文件**：`apps/web/core/services/issue/issue.service.ts:139-145`

```typescript
async getIssueActivities(workspaceSlug, projectId, issueId) {
    return this.get(
        `/api/workspaces/${workspaceSlug}/projects/${projectId}/${this.serviceType}/${issueId}/history/`
        // ⚠️ 不传任何参数！
    )
}
```

**分析**：不传 `activity_type` → ❌ 走默认分支 ④。但经核实，此方法**当前没有被任何地方调用**。

### 2.4 实际触发面评估

| 场景 | 是否触发默认分支 | 触发条件 | 风险 |
|-----|----------------|---------|------|
| 普通用户查看 Issue 时间线 | ❌ | `serviceType=ISSUES`，两个 Service 分别传 `issue-property` / `issue-comment` | 无 |
| Epic 类型 Issue 查看时间线 | ✅ | `serviceType=EPICS`，传 `epic-property` / `epic-comment`，后端不识别 | 🔴 500 |
| `IssueService.getIssueActivities` 被调用 | ✅ | 不传 `activity_type` | 🔴 500（当前无调用方） |
| 第三方 API 客户端不传参数 | ✅ | 请求 `/history/` 不带参数 | 🔴 500 |
| 传入非法 `activity_type` 值 | ✅ | 如 `activity_type=invalid` | 🔴 500 |

### 2.5 Epic 场景的额外风险

后端 `IssueActivityEndpoint` 只识别两个 `activity_type` 值：
- `"issue-property"`（第 66 行）
- `"issue-comment"`（第 77 行）

前端 Epic 场景传入的值：
- `"epic-property"` → 后端不识别 → 默认分支 → TypeError
- `"epic-comment"` → 后端不识别 → 默认分支 → TypeError

**这意味着 Epic 类型的 Issue 时间线功能可能在当前代码中完全不可用**。

---

## 三、默认分支修复思路的代码级对比

### 3.1 当前代码（有 Bug）

**文件**：`apps/api/plane/app/views/issue/activity.py:81-84`

```python
result_list = sorted(
    chain(issue_activities, issue_comments),
    key=lambda instance: instance["created_at"],  # ❌ Model 实例不支持字典访问
)
return Response(result_list, status=status.HTTP_200_OK)
```

**问题**：`issue_activities` 和 `issue_comments` 是原始 Django QuerySet，`chain` 合并后迭代产出 Model 实例，`instance["created_at"]` 抛出 `TypeError`。

### 3.2 方案 A：属性排序后直接 Response

```python
result_list = sorted(
    chain(issue_activities, issue_comments),
    key=lambda instance: instance.created_at,  # ✅ 改为属性访问
)
return Response(result_list, status=status.HTTP_200_OK)
```

#### 可行性分析

| 维度 | 分析 |
|-----|------|
| **排序正确性** | ✅ `instance.created_at` 对 `IssueActivity` 和 `IssueComment` 都有效（均继承 `ProjectBaseModel` → `BaseModel` → 含 `created_at` 字段） |
| **Response 序列化** | ⚠️ `Response(result_list)` 会将 Model 实例列表交给 DRF 的默认渲染器。DRF 的 `JSONRenderer` 对非 dict/list 对象调用 `json.dumps`，而 Django Model 不默认支持 JSON 序列化，会抛出 `TypeError: Object of type IssueActivity is not JSON serializable` |
| **字段完整性** | ⚠️ 即使序列化成功，返回的字段不会包含 `IssueActivitySerializer` / `IssueCommentSerializer` 中定义的嵌套序列化字段（如 `actor_detail`、`comment_reactions`） |

**结论**：方案 A **不可行**。即使修复了 `instance["created_at"]` → `instance.created_at`，排序正确后 Response 仍会因为 Model 实例无法 JSON 序列化而 500。

#### 验证：DRF Response 对 Model 实例的处理

```python
# DRF Response.__init__ 接受任意 data
# JSONRenderer.render 调用 json.dumps(data)
# Django Model 实例不是 JSON serializable
# TypeError: Object of type IssueActivity is not JSON serializable
```

### 3.3 方案 B：先序列化再排序返回

```python
result_list = sorted(
    chain(
        IssueActivitySerializer(issue_activities, many=True).data,  # ✅ 序列化为 dict 列表
        IssueCommentSerializer(issue_comments, many=True).data,     # ✅ 序列化为 dict 列表
    ),
    key=lambda instance: instance["created_at"],  # ✅ dict 支持 [] 访问
)
return Response(result_list, status=status.HTTP_200_OK)
```

#### 可行性分析

| 维度 | 分析 |
|-----|------|
| **排序正确性** | ✅ 序列化后 `instance["created_at"]` 返回字符串（ISO 8601 格式如 `"2024-01-15T10:30:00Z"`），字符串排序与时间排序一致 |
| **Response 序列化** | ✅ `result_list` 是 `list[dict]`，`JSONRenderer` 可直接序列化 |
| **字段完整性** | ✅ 通过各自的 Serializer 序列化，包含 `actor_detail`、`comment_reactions` 等嵌套字段 |
| **性能影响** | ⚠️ 两个 QuerySet 都需要完整序列化后再排序，相比分分支返回内存占用更高 |
| **数据一致性** | ✅ 与 `issue-property` / `issue-comment` 分支返回的数据结构一致 |

**边界问题 1**：`IssueActivitySerializer.data` 和 `IssueCommentSerializer.data` 的字段结构不同，前端需要根据某个字段区分类型。当前合并返回中，评论有 `issue_comment` 字段关联，活动有 `field` 字段，但**没有统一的 `activity_type` 字段**。

**边界问题 2**：`issue-property` 分支额外 `prefetch_related` 了 `issue__issue_intake`（第 67-73 行），默认分支没有预取此关联，可能导致 `source_data` 字段缺失。

**结论**：方案 B **可行但需要补充**。

### 3.4 方案 B 的补充需求

```python
# 默认分支应补全预取关联
issue_activities = issue_activities.prefetch_related(
    Prefetch(
        "issue__issue_intake",
        queryset=IntakeIssue.objects.only("source_email", "source", "extra"),
        to_attr="source_data",
    )
)

# 序列化再排序
result_list = sorted(
    chain(
        IssueActivitySerializer(issue_activities, many=True).data,
        IssueCommentSerializer(issue_comments, many=True).data,
    ),
    key=lambda instance: instance["created_at"],
)
return Response(result_list, status=status.HTTP_200_OK)
```

### 3.5 方案对比总结

| 维度 | 方案 A：属性排序后 Response | 方案 B：先序列化再排序返回 |
|-----|--------------------------|------------------------|
| 修复 `instance["created_at"]` | ✅ 改为 `instance.created_at` | ✅ 序列化后 dict 访问 |
| 排序正确性 | ✅ | ✅ |
| Response JSON 序列化 | ❌ Model 不可 JSON 序列化 | ✅ dict 可直接序列化 |
| 嵌套序列化字段 | ❌ 丢失 `actor_detail` 等 | ✅ 完整保留 |
| 预取关联一致性 | ❌ 缺少 `issue_intake` | ⚠️ 需手动补全 prefetch |
| 前端兼容性 | ❌ 字段结构不兼容 | ✅ 与已有分支一致 |
| 性能 | ✅ 无额外序列化开销 | ⚠️ 双序列化 + 内存排序 |
| 额外工作 | 需为 Model 添加 `to_dict()` | 需补全 `prefetch_related` |

### 3.6 最终建议

**采用方案 B**，完整修复代码：

```python
# 默认分支：合并返回活动与评论
if not request.GET.get("activity_type"):
    # 补全 issue-property 分支的预取关联
    issue_activities = issue_activities.prefetch_related(
        Prefetch(
            "issue__issue_intake",
            queryset=IntakeIssue.objects.only("source_email", "source", "extra"),
            to_attr="source_data",
        )
    )

    result_list = sorted(
        chain(
            IssueActivitySerializer(issue_activities, many=True).data,
            IssueCommentSerializer(issue_comments, many=True).data,
        ),
        key=lambda instance: instance["created_at"],
    )
    return Response(result_list, status=status.HTTP_200_OK)
```

同时建议增加对 `epic-property` / `epic-comment` 的支持，或在 Epic 场景下映射到 `issue-property` / `issue-comment`。

---

## 四、核心文件索引

| 证据 | 文件路径 | 行号 |
|------|---------|------|
| access 类型定义 | `packages/types/src/issues/activity/issue_comment.ts` | 47 |
| EIssueCommentAccessSpecifier | `packages/constants/src/issue/common.ts` | 55-58 |
| CommentCardDisplay 图标标识 | `apps/web/core/components/comments/card/display.tsx` | 110-118 |
| CommentQuickActions 切换操作 | `apps/web/core/components/comments/quick-actions.tsx` | 58-73 |
| CommentCreate 表单选择器 | `apps/web/core/components/comments/comment-create.tsx` | 109-134 |
| showAccessSpecifier 来源 | `apps/web/core/components/issues/issue-detail/issue-activity/root.tsx` | 92,143 |
| showAccessSpecifier 传递链 | `apps/web/core/components/issues/issue-detail/issue-activity/activity-comment-root.tsx` | 30,73 |
| 后端默认分支 Bug | `apps/api/plane/app/views/issue/activity.py` | 81-84 |
| IssueActivityService（Issues 正常） | `apps/web/core/services/issue/issue_activity.service.ts` | 32-37 |
| IssueCommentService（Issues 正常） | `apps/web/core/services/issue/issue_comment.service.ts` | 36-39 |
| IssueService.getIssueActivities（不传参数） | `apps/web/core/services/issue/issue.service.ts` | 139-145 |
| Epic 场景传参 | 上述两个 Service 的 `serviceType === EPICS` 分支 | 34,38 |
